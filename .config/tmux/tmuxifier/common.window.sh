new_window "main"
split_h 30
split_v 50
select_pane 1
# run_cmd "nvim"
select_pane 2
# run_cmd "spt"

# currently running nvim results in logo missing, and spt in invalid 400 API request. Thus disabling them
